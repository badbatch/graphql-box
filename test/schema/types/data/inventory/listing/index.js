import Inventory from '../';

/**
 *
 * The listing inventory data type
 */
export default class ListingInventory extends Inventory {
  /**
   *
   * @constructor
   * @param {Object} data
   * @return {void}
   */
  constructor(data) { // eslint-disable-line no-useless-constructor
    super(data);
  }
}
